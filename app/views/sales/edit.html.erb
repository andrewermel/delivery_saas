<% content_for :title, "Editar Venda" %>

<div class="mb-3">
  <%= link_to "← Voltar", sales_path, class: "btn btn-outline-secondary" %>
</div>

<div class="card shadow">
  <div class="card-header bg-light">
    <h4>Editar Venda</h4>
  </div>
  <div class="card-body">
    <%= render "form", sale: @sale, items: @items %>
  </div>
</div>
